// Importing Nozyx's Maven Repository.
pluginManagement {
    repositories {
        maven {
            name = "nozyx"
            url = "https://maven.nozyx.dev/"
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

// Change this to be the same as your addon ID.
rootProject.name = "strider-example-addon"
